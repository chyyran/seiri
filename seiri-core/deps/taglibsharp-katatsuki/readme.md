# Katatsuki Helper for TagLibSharp

Implements Katatsuki-compatible semantics for *seiri*. 

Required since TagLibSharp has features not implemented in Native TagLib that are infeasible to port to Rust.